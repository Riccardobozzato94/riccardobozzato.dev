/**
 * Font configuration for the Riccardo Bozzato brand site.
 *
 * We load fonts via @import in globals.css for performance
 * (one request, no FOUT), but this file exports CSS class
 * references for use in layout.tsx if needed.
 */
export const fontClasses = "";
